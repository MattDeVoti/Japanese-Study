# macOS
.DS_Store

# Xcode / build artifacts
DerivedData/
build/
*.xcuserstate
xcuserdata/

# XcodeGen generates Omedetou.xcodeproj from project.yml — don't track it
Omedetou.xcodeproj/

# SQLite WAL/backup artifacts (the real dictionary.db is kept)
*.db-shm
*.db-wal
*.db-journal
dictionary_backup.db

# Stray data dumps, and the third-party reference library this app does not
# ship (printable ebooks/flashcards bought from JLPT Sensei) — local only.
combined_vocabulary.txt
/Reference Material/
